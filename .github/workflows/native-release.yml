name: Build Native Images

on:
  push:
    tags:
      - '[0-9]*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. 2.0.3)'
        required: true

jobs:
  build-jar:
    runs-on: ubuntu-latest
    name: Build fat JAR
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Set release version
        if: inputs.tag
        run: ./mvnw versions:set -DnewVersion=${{ inputs.tag }} -q

      - name: Build JAR
        run: ./mvnw -B clean package -DskipTests --no-transfer-progress

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: yj-schema-validator-jar
          path: target/yj-schema-validator-*.jar
          if-no-files-found: error

  build-native:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: yj-schema-validator-linux-amd64
            extension: ''
          - os: ubuntu-24.04-arm
            artifact: yj-schema-validator-linux-arm64
            extension: ''
          - os: macos-13
            artifact: yj-schema-validator-macos-amd64
            extension: ''
          - os: macos-latest
            artifact: yj-schema-validator-macos-arm64
            extension: ''
          - os: windows-latest
            artifact: yj-schema-validator-windows-amd64
            extension: '.exe'

    runs-on: ${{ matrix.os }}
    name: Build ${{ matrix.artifact }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '17'
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Visual Studio shell (Windows only)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Set release version
        if: inputs.tag
        shell: bash
        run: ./mvnw versions:set -DnewVersion=${{ inputs.tag }} -q

      - name: Build native image
        run: ./mvnw -B -Pnative native:compile -DskipTests --no-transfer-progress

      - name: Rename artifact
        shell: bash
        run: mv target/yj-schema-validator${{ matrix.extension }} target/${{ matrix.artifact }}${{ matrix.extension }}

      - name: Smoke test
        shell: bash
        run: |
          ./target/${{ matrix.artifact }}${{ matrix.extension }} --help
          ./target/${{ matrix.artifact }}${{ matrix.extension }} --schema=src/test/resources/testdata/sample-schema.json src/test/resources/testdata/valid.yaml

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: target/${{ matrix.artifact }}${{ matrix.extension }}

  publish-release:
    needs: [build-jar, build-native]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag || github.ref_name }}
          files: |
            artifacts/yj-schema-validator-jar/*.jar
            artifacts/yj-schema-validator-linux-amd64/*
            artifacts/yj-schema-validator-linux-arm64/*
            artifacts/yj-schema-validator-macos-amd64/*
            artifacts/yj-schema-validator-macos-arm64/*
            artifacts/yj-schema-validator-windows-amd64/*
